/// Configuration settings for offline functionality
class OfflineConfig {
  /// Whether to automatically sync when online
  final bool autoSync;

  const OfflineConfig({
    this.autoSync = true,
  });
}
